{
  "name": "postspec",
  "version": "0.1.0",
  "description": "Initialize an agent skill that automatically creates and updates project specs",
  "type": "module",
  "bin": {
    "postspec": "./bin/postspec.js"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "scripts": {
    "test": "node --test",
    "lint": "node --check bin/postspec.js && node --check src/cli.js && node --check src/core.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
